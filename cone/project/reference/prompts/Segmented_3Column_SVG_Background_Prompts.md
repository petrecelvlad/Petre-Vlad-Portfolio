```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: bubblegum pink. Scatter a handful of simple, flat pixel-art heart icons across the scene in 2-3 depth layers (near, mid, far), each layer a different size — the kind of blocky heart used as a health/lives icon in an old arcade game, not a soft realistic heart. Keep every heart low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — no gradients or texture needed, just flat pink at varying tints.

Invent your own movement for the hearts — don't default to a plain drift, spin, or bounce; come up with something distinctive and a little surprising. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: lime green. Scatter a handful of simple, flat, faceted loot-gem icons across the scene in 2-3 depth layers (near, mid, far), each layer a different size — the kind of RPG treasure gem you'd find dropped in a dungeon-crawler or platformer, not a realistic jewel. Keep every gem low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — no gradients or texture needed, just flat green at varying tints.

Invent your own movement for the gems — don't default to a plain drift, spin, or bounce; come up with something distinctive and a little surprising. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: sunset orange. Scatter a handful of simple, flat power-up star bursts across the scene in 2-3 depth layers (near, mid, far), each layer a different size — the kind of screen-clearing bonus-star icon from a retro shoot-em-up, not a soft realistic sparkle. Keep every star low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — no gradients or texture needed, just flat orange at varying tints.

Invent your own movement for the stars — don't default to a plain drift, spin, or bounce; come up with something distinctive and a little surprising. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: royal violet. Scatter a handful of simple, flat collectible coin/gem tokens across the scene in 2-3 depth layers (near, mid, far), each layer a different size — the kind of currency-pickup icon from a platformer or arcade collector, not a realistic coin. Keep every token low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — no gradients or texture needed, just flat violet at varying tints.

Invent your own movement for the tokens — don't default to a plain drift, spin, or bounce; come up with something distinctive and a little surprising. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: sky blue. Scatter a handful of blocky, pixel-art clouds across the scene in 2-3 depth layers (near, mid, far), each layer a different size — built from chunky square-ish lumps like an 8-bit game's world-map sky, not a soft realistic cloud. Keep every cloud low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — no gradients or texture needed, just flat blue at varying tints.

Have every cloud travel steadily from the bottom of the scene to the top, looping back to the bottom the instant it exits so the motion is a perfectly seamless closed loop — no jump cut, no fade, no visible reset. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```isometric
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: sky blue. Scatter a handful of isometric clouds across the scene in 2-3 depth layers (near, mid, far), each layer a different size — built like a faceted 3D block with distinct top/left/right shaded faces, the way clouds or terrain tiles look in an isometric mobile strategy game, not a flat realistic cloud. Keep every cloud low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — flat shaded tints only, no photographic texture.

Have every cloud travel steadily from the bottom of the scene to the top, looping back to the bottom the instant it exits so the motion is a perfectly seamless closed loop — no jump cut, no fade, no visible reset. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: sky blue. Scatter a handful of bold, cartoony clouds across the scene in 2-3 depth layers (near, mid, far), each layer a different size — thick rounded outline, bumpy bubble-lobed silhouette, the kind of chunky cloud you'd see in a juicy casual mobile game's title screen. Keep every cloud low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — flat fill only, no gradients or texture.

Have every cloud travel steadily from the bottom of the scene to the top, looping back to the bottom the instant it exits so the motion is a perfectly seamless closed loop — no jump cut, no fade, no visible reset. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```

---

```markdown
Design a small, looping SVG animation, animated with CSS only (transform/opacity @keyframes in an inline <style> block — no JavaScript, no SMIL, never toggle `display`). It must loop seamlessly forever.

Build it entirely around one color: sky blue. Scatter a handful of extremely minimal clouds across the scene in 2-3 depth layers (near, mid, far), each layer a different size — nothing more than two or three overlapping soft-edged circles/ellipses per cloud, no outline, no detail, as plain and simple as a cloud shape can get. Keep every cloud low-opacity (roughly 0.08-0.35) so it reads as quiet background texture, not foreground content — flat fill only, no gradients or texture.

Have every cloud travel steadily from the bottom of the scene to the top, looping back to the bottom the instant it exits so the motion is a perfectly seamless closed loop — no jump cut, no fade, no visible reset. Use all 2-3 depth layers with a real parallax effect: the nearer, larger layer moves noticeably more than the farther, smaller layer, and each layer runs on its own staggered timing so no two ever move in lockstep. It needs to feel calm and cute.
```
